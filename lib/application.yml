server:
  port: 8080

logging:
  level:
    root: INFO

berrywave:
  license:
    company: (not configured)
  edi:
    syntax:
      delimiter: "*"
      segment-terminator: "~"
      segment-terminator-suffix: LF
