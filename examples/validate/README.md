# Validation Examples

Examples in this directory demonstrate the BerryWave Python EDI SDK
`validate()` API.

Validation performs EDI compliance checking and is separate from technical
acknowledgments generated by `acknowledge()`.

The Community Edition included with this SDK release does not include
validation capabilities. The examples document the API and will raise a
feature availability exception when executed with the Community Edition.

Enterprise Edition users will be able to use validation examples directly.