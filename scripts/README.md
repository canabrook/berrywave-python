# Build Scripts

These scripts are used internally to build BerryWave EDI Python SDK releases.

## build_release.py

Creates release artifacts in the `dist/` directory:

- Python wheel (`.whl`)
- Example archive (`.zip`)

The script is intended for maintainers preparing GitHub releases.